/**
 * Header.tsx — legacy component (kept for backward compat)
 * The portal uses PageBanner and SectionHeader for page headers.
 * This component is no longer used in the main portal layout.
 */

// Re-export shared layout components for convenience
export { default as PageBanner } from './PageBanner';
export { default as SectionHeader } from './SectionHeader';
